Place your splash screen image here as `splash_logo.png`.
Recommended size: about 1200x1200 px. The generator uses `assets/images/splash_logo.png` path.

After adding the image, run:

```bash
dart run flutter_native_splash:create
```

Then rebuild the app (no hot reload) with `flutter run`.